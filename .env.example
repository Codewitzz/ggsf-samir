# Google AI Studio / Gemini API (browser — restrict key by HTTP referrer in Google Cloud)
# If chat fails with quota errors, enable billing or use a key with remaining free tier on gemini-2.5-flash.
VITE_GEMINI_API_KEY=

# Supabase shared admin data sync
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_PUBLISHABLE_KEY=
