[build]
  command = "npm run build"
  publish = "dist"

# Single-page app (HashRouter works without this, but safe if you switch to BrowserRouter)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
